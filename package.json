{
  "name": "nodalkit-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "build:core": "bun run --filter @nodalkit/nodalkit-core build",
    "build:cli": "bun run --filter @nodalkit/nodalkit build",
    "build:localmcp": "bun run --filter @nodalkit/localmcp build",
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "check": "bun run format && bun run lint && tsc --noEmit",
    "dev:cli": "bun run packages/cli/src/index.ts",
    "dev:localmcp": "bun run packages/localmcp/src/index.ts",
    "dev:remotemcp": "bun run apps/remotemcp/src/index.ts",
    "release:pack:core": "bun run --filter @nodalkit/nodalkit-core pack:dry",
    "release:pack:cli": "bun run --filter @nodalkit/nodalkit pack:dry",
    "release:pack:localmcp": "bun run --filter @nodalkit/localmcp pack:dry"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
